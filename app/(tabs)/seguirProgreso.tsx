//Hay que ver como hacemos esto
//1. va al mapa de las actividades y sus pasos y selecciona la actividad en la que estaba
//2. lo manda directo a la actividad
//Si es la opción 1 habría que hacer lo de la rotación a horizontal

import { supabase } from '@/lib/supabase';
import React from 'react';

import { FlatList, Image, Button, ScrollViewBase, View, Text } from 'react-native';

const SeguirProgresoScreen = () =>{

    return(
        <View>
            <Text>Hola están en seguir progreso</Text>
        </View>
    );

};

export default SeguirProgresoScreen;